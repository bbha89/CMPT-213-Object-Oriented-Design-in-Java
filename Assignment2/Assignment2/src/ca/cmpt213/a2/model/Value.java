package ca.cmpt213.a2.model;

/**
 * Shows the value state in each
 * of the cells in the maze
 */
public enum Value {
        HERO,
        WALL,
        EMPTY_SPACE,
        MONSTER,
        POWER,
        DEAD,
        HIDE_WALL,
        HIDE_EMPTY
    }
