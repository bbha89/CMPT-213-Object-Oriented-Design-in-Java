package ca.cmpt213.a2.model;

/**
 * The value states that each cell in the maze can have
 */
public enum Value {
        HERO,
        WALL,
        EMPTY_SPACE,
        MONSTER,
        POWER,
        DEAD,
        HIDE_WALL,
        HIDE_EMPTY
    }
